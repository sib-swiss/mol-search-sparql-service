#!/usr/bin/env python3
import sys
import os

# Add src to path to allow running without installation
sys.path.insert(0, os.path.join(os.path.dirname(__file__), "src"))

from mol_search_sparql_service.main import main

if __name__ == "__main__":
    main()
